# EmotionHub

### 基于图神经网络情感传染模型与实时 Feed 流优化的社交媒体深度分析与推荐平台

> *"在情绪的海洋里，我们为你找到属于你的那一束光。"*

---

**一句话介绍**：EmotionHub 是一个将"情感传染理论"融入社交推荐系统的平台，让算法不仅读懂你的兴趣，更读懂你此刻的心情。

---

## 产品愿景

> 在情绪的海洋里，我们为你找到属于你的那一束光。

---

## 一、开发背景

### 1.1 问题起源

在当下的社交媒体环境中，用户每天被动接收大量内容，但平台推荐逻辑几乎只围绕"用户感兴趣什么"展开——谁在意过你**此刻的心情**？

一个真实场景：当用户在经历情绪低落时，平台仍然推送引发焦虑的负面内容；当用户心情愉悦时，却因为"协同过滤"的茧房效应，持续困在单一情感维度的信息中。

更深层的问题是：**情感在社交网络中如何流动？哪些人更容易影响他人的情绪？相似的情感倾向是否能自动汇聚成社区？**

这些在现有社交平台中几乎无人回答。

### 1.2 我们想做什么

EmotionHub 是一个以**情感智能**为核心的社交媒体深度分析平台：

- **对普通用户**：提供"情感自适应"的智能 Feed 流，不再被算法困在情绪茧房里
- **对研究者**：提供情感传播链追踪、用户影响力量化、情感社区发现等分析工具
- **对平台运营者**：提供多维情感统计和可管控的内容生态

### 1.3 技术挑战

| 挑战               | 描述                                           |
| ------------------ | ---------------------------------------------- |
| 如何量化"情感"本身 | 将文本内容转化为可计算的情感分数与标签体系     |
| 如何建模情感传播   | 评论如何继承或反转帖子的情感？传播链如何追踪？ |
| 如何发现情感社区   | 无监督地从海量交互中自动发现情感相似的用户群体 |
| 如何融合情感与推荐 | 将情感维度嵌入推荐算法，而非简单叠加           |
| 如何保证实时性     | Feed 流低延迟 + 情感状态实时更新               |
| 如何安全管理 LLM Key | 多租户场景下用户自主配置 API Key 的安全隔离与优先级管理 |

---

## 二、竞品分析与差异化定位

> *"我们不与巨头争流量，只在情感智能的深处，凿出一束光。"*

### 2.1 竞品选择说明

我们选择与以下同样关注"情感"或"深度社交关系"的小众产品进行对比：

| 竞品                         | 类型             | 定位                                         |
| ---------------------------- | ---------------- | -------------------------------------------- |
| **WeMap（情绪地图）**  | 情感可视化工具   | 汇聚大众情绪热力图，但无社交关系和个性化推荐 |
| **Moodnotes / Daylio** | 情感记录工具     | 个人情绪日记，无社区无推荐                   |
| **同类学术平台**       | 情感分析研究工具 | 多为离线分析工具，无实时 Feed 流能力         |

> 注：抖音、小红书等成熟商业平台已高度产品化，与其直接对比无意义——我们的定位是做"情感智能"赛道的垂直深度工具，而非与其争夺同一市场。

### 2.2 差异化对比

| 维度                     | WeMap / 情绪地图 | Moodnotes / Daylio | EmotionHub                        |
| ------------------------ | ---------------- | ------------------ | --------------------------------- |
| **内容情感分析**   | 仅可视化群体情绪 | 仅记录个人情绪     | 自动分析每条帖子/评论的情感       |
| **情感推荐**       | 无               | 无                 | 情感状态自适应 Feed 流            |
| **传播建模**       | 无               | 无                 | 情感传播链追踪与转折点识别        |
| **社区发现**       | 无               | 无                 | Louvain 算法自动发现情感社区      |
| **用户影响力量化** | 无               | 无                 | PageRank 变体影响力指数           |
| **实时 Feed**      | 无               | 无                 | Socket.io 实时推送 + 情感调节排序 |
| **可视化**         | 地图热力图       | 日历打卡           | 多维 ECharts 图表 + 情感网络图    |

### 2.3 差异化壁垒

```
┌──────────────────────────────────────────────────┐
│              EmotionHub 核心差异化壁垒              │
├──────────────────────────────────────────────────┤
│                                                   │
│  ① 情感-推荐深度闭环                                │
│     业界独特的"用户情感状态 → 内容情感匹配"推荐策略      │
│     不是"你看过什么"，而是"你现在需要什么情感内容"      │
│                                                   │
│  ② Louvain 情感社区自动发现                          │
│     基于余弦相似度构建情感共鸣网络，                  │
│     无监督发现乐观派/理性派/悲观派社区                │
│                                                   │
│  ③ 情感传播白盒化                                    │
│     可追踪每条评论对原帖情感的继承/放大/转折关系，       │
│     可视化呈现传播时间线和转折节点                    │
│                                                   │
│  ④ 多租户 LLM API Key 配置                          │
│     支持多平台（通义千问/文心一言/智谱），              │
│     用户自主配置 → 平台默认 → 配置兜底，三级优先级      │
│                                                   │
│  ⑤ 小团队轻量化落地                                  │
│     不依赖 Kafka/Flink 等重型基础设施，               │
│     MySQL + Redis + 定时任务即可支撑中等规模用户       │
│                                                   │
└──────────────────────────────────────────────────┘
```

---

## 三、团队组成与分工

### 3.1 团队架构（5 人）

```
EmotionHub 团队
│
├── 👤 队长 / 后端负责人
│     负责：系统架构设计、核心算法实现（情感分析/传播建模）、
│           推荐引擎开发、Docker 部署
│     技术栈：Java / Spring Boot / MyBatis-Plus / Redis
│
├── 👤 后端开发 × 1
│     负责：用户认证模块、帖子/评论 CRUD、通知系统、
│           管理后台后端接口
│     技术栈：Java / Spring Boot / JWT / MySQL
│
├── 👤 前端负责人 / 全栈开发
│     负责：Vue 3 项目架构、组件化开发、ECharts 可视化、
│           Socket.io 实时通知、前端工程化
│     技术栈：Vue 3 / TypeScript / Pinia / Element Plus / ECharts
│
├── 👤 算法工程师
│     负责：情感分析引擎（关键词 + LLM）、协同过滤 ALS 矩阵分解、
│           Louvain 社区发现、情感传播算法实现
│     技术栈：NLP / 机器学习算法实现 / Redis 缓存优化
│
└── 👤 测试 / 运维 / 文档
      负责：单元测试编写、Docker Compose 部署维护、
            Flyway 数据库迁移管理、产品文档撰写
      技术栈：Docker / MySQL / Redis / JUnit
```

### 3.2 分工总览

| 角色          | 核心交付物                                   |
| ------------- | -------------------------------------------- |
| 队长/后端架构 | 推荐引擎、情感传播建模、Docker 部署          |
| 后端开发      | 用户/帖子/评论 CRUD、通知模块、管理后台接口  |
| 前端负责人    | 全部 Vue 页面、情感可视化图表、实时通知      |
| 算法工程师    | 情感分析引擎、Louvain 社区发现、ALS 协同过滤 |
| 测试/运维     | 单元测试、Docker 编排、数据库迁移            |

---

## 四、主要创新点

> *"算法写下的，是情感的方程式；我们求解的，是人心的温度。"*

### 创新点一：用户情感状态自适应推荐引擎

**核心思想**：推荐系统不仅要懂"你兴趣什么"，更要懂"你此刻心情如何"。

我们设计了完整的**情感状态识别 → 内容情感匹配 → 推荐策略选择**闭环：

```
用户情感状态识别
│
├── 滑动窗口统计（最近 24h 帖子情感分数加权均值）
│
└── 状态分类机
      ├── LOW（情绪低落）
      │     → 策略：推送高正向情感内容（治愈系、正能量）
      │
      ├── ANXIOUS（焦虑）
      │     → 策略：推送中性平静内容（减少情感刺激）
      │
      ├── HAPPY（愉悦）
      │     → 策略：全量推送（情感多元化，防止茧房）
      │
      ├── CALM（平静）
      │     → 策略：推送温和积极内容（保持情绪稳定）
      │
      └── FLUCTUANT（波动）
            → 策略：推送情感中性内容（稳定情绪）
```

**互补疗愈推荐公式**：

```
content.emotionScore > 0.6  &&  tag in [POSITIVE_ENERGY, HEALING]  →  推荐给 LOW 状态用户
|emotionScore| < 0.2  &&  tag == NEUTRAL_CALM                    →  推荐给 ANXIOUS 用户
```

最终推荐评分：`finalScore = 0.5 × 热度分 + 0.3 × 情感匹配分 + 0.2 × 时效分`

---

### 创新点二：Louvain 算法驱动的情感社区发现

**核心思想**：具有相似情感特征的人会自动聚集成社区——我们用算法把这件事"白盒化"。

**算法流程**：

```
Step 1: 情感特征向量构建
        每个用户 → 4维向量 [avgScore, positiveRatio, neutralRatio, negativeRatio]

Step 2: 余弦相似度计算
        sim(A, B) = dot(vA, vB) / (|vA| × |vB|)
        归一化至 [0, 1]

Step 3: 共鸣边构建
        sim ≥ 0.3 → 建立无向边，权重 = sim分值

Step 4: Louvain 社区发现（贪心变体）
        迭代地将每个节点移入使"社区内总权重增加最大"的社区
        迭代上限 30 次，防止死循环

Step 5: 社区分类
        avgScore > 0.2 → 乐观派（OPTIMISTIC）
        avgScore < -0.2 → 悲观派（PESSIMISTIC）
        其余 → 理性派（RATIONAL）
```

**实际意义**：平台可以清晰看到"悲观派社区"的用户构成，从而在内容推荐时进行精准的情感干预。

---

### 创新点三：情感传播链建模与转折点识别

**核心思想**：每一层评论对原帖情感做了什么？是被"放大"还是被"衰减"？在哪里发生了情感反转？

**核心指标**：

| 指标 | 计算公式 | 含义 |
| -- | -- | -- |
| **情感一致性** | `1 - |postScore − commentScore|` | 衡量评论情感与原帖的接近程度，取值 [0, 1]，越接近 1 表示越一致 |
| **情感放大系数** | `commentScore / postScore` | 评论情感强度相对于原帖的倍率，> 1 表示放大，< 1 表示衰减 |
| **情感转折点** | `sign(postScore) ≠ sign(commentScore)` | 当评论与原帖情感符号相反时，标记为转折点，追踪传播链中的情感反转 |

**核心公式详解**：

```
情感一致性（Consistency）：
    C = 1 - |postScore - commentScore|
    其中 postScore、commentScore ∈ [-1.0, 1.0]
    → C ∈ [0, 1]，越接近 1 表示评论情感与原帖越接近

情感放大系数（Amplification）：
    A = commentScore / postScore（当 postScore ≠ 0）
    A > 1.0：情感被放大（如原帖 0.5 → 评论 0.8）
    A < 1.0：情感被衰减（如原帖 0.5 → 评论 0.2）
    A < 0：发生情感反转（原帖正向 → 评论负向，或反之）
```

**传播类型判定**：

```
高一致性（C > 0.7）+ 放大（A > 1.0）→ AMPLIFIED（放大传播）
    原帖情感被评论进一步加强，如正面内容引发更强烈的正面评论

高一致性（C > 0.7）+ 低放大（A < 1.0）→ CONSISTENT（一致传播）
    评论与原帖情感方向一致，强度相近，情感稳定传递

低一致性（C < 0.4）+ 多次转折 → CONTROVERSIAL（争议传播）
    评论与原帖情感差异大或反复横跳，引发社区分化

放大系数 < 0（A < 0）          → REVERSED（反转传播）
    评论情感与原帖完全相反（正→负 或 负→正），标记为情感转折点
```

---

### 创新点四：协同过滤 + 情感感知双塔推荐

**核心思想**：推荐既要看"你跟谁口味相似"，也要看"你跟谁情感相投"。

**技术实现**：

```
候选集生成层
│
├── Redis 缓存层（每日凌晨 2:30 自动重建）
│     ├── 用户个性化推荐列表（ZSet，Top 200）
│     └── 热点帖子排行
│
├── ALS 矩阵分解（10维隐向量，8次迭代）
│     └── 用户-帖子交互矩阵分解
│
└── 推荐评分融合
      ├── 兴趣相似度（余弦，权重 60%）
      │     基于点赞/评论/发帖行为矩阵
      └── 情感向量相似度（余弦，权重 40%）
            基于 [avgScore, posRatio, neuRatio, negRatio]

多样性保障
├── 同一作者最多占 25%
├── 同一情感标签最多占 60%
└── 剩余按 finalScore 降序补位
```

---

### 创新点五：多 LLM 集成的情感分析管道

**核心思想**：用 AI 大模型替代规则词典，提升情感分析的语义理解能力，并支持多用户提供自主配置。

```
情感分析请求
      │
      ├── 模式1：关键词分析（默认，毫秒级响应）
      │     情感词典 + 否定词处理
      │
      └── 模式2：通义千问 LLM（配置 API Key 后启用）
            ├── API Key 优先级：用户个人配置 > 平台默认配置 > 配置文件
            ├── Prompt 工程：Few-shot 角色设定 + 中文口语情感规则
            ├── 调侃语气后处理：笑死/哈哈等 → 避免误判为强负向
            ├── 熔断降级：LLM 失败 → 自动回退关键词分析
            └── 结果缓存：24h TTL，同一内容不重复计费

支持扩展：
├── 文心一言（百度）
└── 智谱 GLM-4（国内）
```

**API Key 优先级机制**（三层兜底）：

```
① 用户个人配置（user_id > 0，最高优先）
      ↓ 未配置或未启用
② 平台默认配置（user_id = NULL）
      ↓ 未配置
③ 配置文件（application.yml 中的 dashscope.api-key）
```

> 注意：用户配置通过 `api_key_config` 表管理，支持多平台（qianwen/openai/wenxin/zhipu），API Key 返回前端时自动掩码（只显示前4位 + **** + 后4位）。

---

## 五、技术栈

### 5.1 系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                        前端（Vue 3）                          │
│     Vue Router / Pinia / Element Plus / ECharts / Socket.io   │
└──────────────────────────────┬───────────────────────────────┘
                               │ HTTP / WebSocket
                               ▼
┌──────────────────────────────────────────────────────────────┐
│                    Nginx 反向代理 + 静态资源                    │
│                      /api → :8080  / → :3000                  │
└──────────────────────────────┬───────────────────────────────┘
                               ▼
┌──────────────────────────────────────────────────────────────┐
│                  Spring Boot 应用层（Java 17）                  │
│                                                               │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│   │ 用户/认证模块  │  │ 帖子/评论模块  │  │ 情感分析模块   │       │
│   ├─────────────┤  ├─────────────┤  ├─────────────┤       │
│   │ 推荐算法模块  │  │ Louvain社区  │  │ 传播建模模块  │       │
│   ├─────────────┤  ├─────────────┤  ├─────────────┤       │
│   │ 通知实时模块  │  │ 管理后台模块  │  │ API Key配置  │       │
│   └─────────────┘  └─────────────┘  └─────────────┘       │
│                                                               │
│            MyBatis-Plus ORM  │  Knife4j API 文档              │
└──────────────────────────────┬───────────────────────────────┘
                               ▼
         ┌──────────────────────┼──────────────────────┐
         ▼                      ▼                      ▼
  ┌─────────────┐       ┌─────────────┐       ┌─────────────────┐
  │  MySQL 8.0  │       │  Redis 7.0  │       │  通义千问 API    │
  │  数据持久化  │       │缓存/会话/热榜│       │  LLM 情感分析   │
  │ Flyway迁移  │       │ ZSet/List   │       │                 │
  │+api_key_config│      │ BloomFilter │       └─────────────────┘
  └─────────────┘       └─────────────┘
```

### 5.2 技术选型总表

| 层级       | 技术              | 版本   | 用途         |
| ---------- | ----------------- | ------ | ------------ |
| 前端框架   | Vue.js            | 3.4    | 核心框架     |
| 类型系统   | TypeScript        | 5.3    | 类型安全     |
| 状态管理   | Pinia             | 2.1.7  | 状态管理     |
| UI 组件    | Element Plus      | 2.5    | 页面组件     |
| 数据可视化 | ECharts           | 5.4.3  | 图表与网络图 |
| 实时通信   | Socket.io         | 4.6.1  | 通知推送     |
| 构建工具   | Vite              | 5.0    | 前端构建     |
| 后端框架   | Spring Boot       | 3.2.2  | 核心框架     |
| 编程语言   | Java              | 17     | 开发语言     |
| ORM 框架   | MyBatis-Plus      | 3.5.5  | 数据访问     |
| 数据库     | MySQL             | 8.0    | 数据存储     |
| 缓存       | Redis             | 7.0    | 缓存与热榜   |
| 数据库迁移 | Flyway            | 9.22.3 | 版本管理     |
| API 文档   | Knife4j           | 4.4.0  | 接口文档     |
| 身份认证   | JWT (jjwt)        | 0.12.5 | 无状态认证   |
| 连接池     | Druid             | 1.2.20 | 连接池       |
| 工具库     | Hutool            | 5.8.24 | 工具集       |
| JSON       | FastJSON2         | 2.0.45 | JSON处理     |
| LLM SDK    | DashScope         | 2.12.0 | 通义千问     |
| 容器化     | Docker Compose    | —     | 一键部署     |
| 代码规范   | ESLint + Prettier | —     | 代码质量     |

---

## 六、项目亮点

> *"流畅如流水，规范如乐章，新颖如晨曦。"*

### 6.1 运行流畅

- **Redis 三级缓存**：热点帖子缓存（1小时）、用户推荐列表（ZSet）、情感分析结果（24h TTL），API 响应时间 < 100ms
- **异步任务池**：情感分析、推荐重算等耗时任务异步执行，用户无感知等待
- **定时任务自动化**：每日凌晨 2:30 自动重建协同过滤矩阵，每日 1:00 汇总情感统计，每周日凌晨 2:00 重算情感共鸣网络
- **连接池精细调优**：Druid（最大 20 连接）、Lettuce（最大 20 连接），高并发下稳定运行
- **缓存一致性保障**：情感分析完成后立即清除用户/平台统计数据缓存，确保统计页面实时准确
- **事务-异步协同优化**：发帖事务提交后再触发异步情感分析，通过 `TransactionSynchronization.afterCommit()` 避免异步线程读取到未提交数据

### 6.2 整体协调

- **前后端完全分离**：RESTful API 解耦，前后端独立迭代，通过 Knife4j 在线调试
- **模块化 Maven 多模块**：common / model / dao / service / web 五层，职责边界清晰
- **Docker Compose 一键编排**：MySQL + Redis + Backend + Frontend 四个容器，接近生产级部署
- **统一返回格式**：`Result<T>` 封装所有接口响应，前端统一处理

### 6.3 开发规范

- **Flyway 数据库版本管理**：所有数据库变更通过 migration 脚本管理，团队协作无迁移冲突
- **统一异常处理**：`GlobalExceptionHandler` 全局捕获，返回标准化错误码（ErrorCode 枚举）
- **日志结构化分级**：DEBUG / INFO / WARN / ERROR 分级输出，生产可追踪
- **前端代码规范**：ESLint + Prettier 统一风格，TypeScript 类型守卫减少运行时错误
- **API 文档自动化**：Knife4j 自动生成 Swagger 文档，每次代码提交后可立即调试

### 6.4 创意新颖

| 维度               | 创新点                                                               |
| ------------------ | -------------------------------------------------------------------- |
| **理论创新** | 首次将"情感传染"理论应用于 Feed 流推荐，提出"情感状态自适应"推荐范式 |
| **算法创新** | 基于余弦相似度的情感共鸣网络 + Louvain 无监督社区发现，无需人工标注  |
| **工程创新** | 多 LLM 提供商统一抽象（ApiKeyConfig）、三层优先级降级、熔断降级缓存  |
| **交互创新** | ECharts 情感趋势折线图、情感社区力导向图、传播时间线图三屏联动       |
| **稳定性创新** | 事务-异步协同（afterCommit）、批量 UPDATE 优化、N+1 查询消除         |

---

## 七、核心功能模块

| #  | 模块                  | 功能描述                                      |
| -- | --------------------- | --------------------------------------------- |
| 1  | **用户认证**    | 注册/登录/JWT/角色权限（USER / ADMIN）        |
| 2  | **帖子广场**    | 发帖/浏览/情感标签过滤/分页                   |
| 3  | **评论互动**    | 嵌套评论/点赞/情感传播记录                    |
| 4  | **情感分析**    | 关键词 + 通义千问双引擎 / 多标签分类          |
| 5  | **情感统计**    | 趋势分析 / 共鸣分析 / 传播分析                |
| 6  | **智能推荐**    | 协同过滤 + ALS + 情感自适应 + 多样性排序      |
| 7  | **情感社区**    | Louvain 社区发现 / 共鸣网络 BFS 扩展 / 可视化 |
| 8  | **实时通知**    | Socket.io 推送 / 未读计数 / 一键已读（批量 UPDATE）|
| 9  | **管理后台**    | 用户管理 / 帖子审核 / 举报处理 / 操作日志     |
| 10 | **Docker 部署** | 四容器编排 / 数据持久化 / 环境隔离            |
| 11 | **LLM Key 管理**| 多平台 API Key 配置 / 个人/平台级切换 / 掩码展示 |

---

## 八、未来展望

| 方向                        | 说明                                                     |
| --------------------------- | -------------------------------------------------------- |
| **图神经网络（GNN）** | 在 Louvain 基础上引入 GNN 做更深入的社交网络情感传播建模 |
| **实时 Feed 流**      | 引入消息队列（Kafka/RabbitMQ）实现真正毫秒级 Feed 推送   |
| **情感干预系统**      | 检测到持续负面情绪时，主动触发正能量内容推送             |
| **跨平台聚合**        | 接入更多社交媒体数据，构建更完整的全网情感画像           |

---

> *"情感的涟漪，终将汇成海洋。"*
>
> *EmotionHub——让每一份情感，被真正看见。*

---

> **文档更新时间**：2026 年 3 月 26 日
